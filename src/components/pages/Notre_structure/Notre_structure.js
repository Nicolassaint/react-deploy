import React from 'react';
import { Button } from '../../Button/Button';

export default function Notre_structure() {
  return (
    <div>
      Notre Structure
      <Button buttonSize='btn--wide' buttonColor='blue'>
        Déposer votre projet
      </Button>
    </div>
  );
}
